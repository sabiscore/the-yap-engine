{"id":"py-bug-01","language":"python","task":"Fix the off-by-one bug in count_complete. The function must return the number of complete records whose status is exactly 'complete'. Preserve the public function signature.","files":{"src/counter.py":"def count_complete(records):\n    return sum(1 for record in records[:-1] if record.get('status') == 'complete')\n"},"validation":["python -c \"from src.counter import count_complete; assert count_complete([{'status':'complete'},{'status':'pending'},{'status':'complete'}]) == 2\""],"success":"count_complete returns 2 for two complete records including the final record."}
{"id":"py-contract-02","language":"python","task":"Harden parse_limit. It must accept a positive integer or numeric string and return an integer. Zero, negative values, booleans, floats, empty strings and non-numeric strings must raise ValueError. Preserve the public function signature.","files":{"src/limits.py":"def parse_limit(value):\n    return int(value)\n"},"validation":["python -c \"exec(\\\"from src.limits import parse_limit\\\\nassert parse_limit(3)==3 and parse_limit('7')==7\\\\nfor x in [0,-1,True,1.5,'','abc']:\\\\n  try: parse_limit(x); raise AssertionError(x)\\\\n  except ValueError: pass\\\")\""],"success":"parse_limit enforces the complete contract without changing its signature."}
{"id":"ts-bug-03","language":"typescript","task":"Fix normalizeTags so it accepts a string or string array, trims whitespace, removes empty values, de-duplicates while preserving first-seen order, and always returns string[]. Do not change the exported function name.","files":{"src/tags.ts":"export function normalizeTags(value: string | string[]): string[] {\n  const values = Array.isArray(value) ? value : [value];\n  return values.map((value) => value.trim());\n}\n"},"validation":["node -e \"const fs=require('fs'); const s=fs.readFileSync('src/tags.ts','utf8'); if(!s.includes('Set')) process.exit(1); if(!s.includes('filter')) process.exit(1);\""],"success":"Implementation trims, filters empty tags and preserves unique first-seen order."}
{"id":"json-contract-04","language":"json","task":"Correct the configuration contract. timeoutMs must be an integer from 1000 through 300000 inclusive. retries must be an integer from 0 through 3. enabled must remain boolean. Do not add unrelated keys.","files":{"config.json":"{\n  \"timeoutMs\": 0,\n  \"retries\": 9,\n  \"enabled\": \"true\"\n}\n"},"validation":["python -c \"import json; c=json.load(open('config.json')); assert isinstance(c['timeoutMs'],int) and 1000 <= c['timeoutMs'] <= 300000; assert isinstance(c['retries'],int) and 0 <= c['retries'] <= 3; assert isinstance(c['enabled'],bool); assert set(c)=={'timeoutMs','retries','enabled'}\""],"success":"Configuration contains only the three required keys with valid types and ranges."}
{"id":"multi-file-05","language":"mixed","task":"Repair the producer/consumer contract across both files. producer.py must emit a dictionary with key 'kind' and integer key 'count'. consumer.py must return the count when kind is 'batch', otherwise raise ValueError. Preserve both public function names.","files":{"producer.py":"def make_event(count):\n    return {'type': 'batch', 'count': str(count)}\n","consumer.py":"def consume(event):\n    if event.get('kind') == 'batch':\n        return event['count']\n    raise ValueError('unsupported event')\n"},"validation":["python -c \"from producer import make_event; from consumer import consume; e=make_event(4); assert e=={'kind':'batch','count':4}; assert consume(e)==4\""],"success":"Both modules agree on the same typed event contract."}
{"id":"refactor-06","language":"python","task":"Refactor safely without changing behavior. Extract the predicate used by eligible_users into a private helper named _is_eligible. Keep eligible_users behavior identical and preserve its public signature.","files":{"src/users.py":"def eligible_users(users):\n    return [u for u in users if u.get('active') is True and u.get('age', 0) >= 18]\n"},"validation":["python -c \"from src.users import eligible_users; u=[{'id':1,'active':True,'age':20},{'id':2,'active':False,'age':30},{'id':3,'active':True,'age':17}]; assert eligible_users(u)==[u[0]]\""],"success":"Behavior is preserved and the predicate is extracted to _is_eligible."}
