node_modules

# Python cache artifacts
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
.eggs/
.env
.env.local
env.local

# Virtual environments
.venv/
venv/
env/

# Test and lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Turborepo
.turbo/

# Runtime video artifacts and render workspaces (repo root and nested under apps/)
/.swarmx/video/
apps/**/.swarmx/video/

# Runtime evolution state — written by the orchestrator on every cycle
/.swarmx/evolution-layer/

# Editor
.vscode/settings.json

# Claude Code — per-user Bash allowlist (project-shared config is settings.json)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db
AGENTS.md
.vercel
.env*

# AI agent session workspaces — ephemeral orchestration artifacts, not source
.agents/teamwork_preview_*/
